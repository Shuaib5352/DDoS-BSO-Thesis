# دليل التنفيذ الشامل
## مشروع الماجستير: كشف هجمات DDoS باستخدام Framework تعلم آلي مختلط محسّن ببروح الخفافيش (BSO)

**المؤلف:** SHUAIB AYAD JASIM  
**التاريخ:** فبراير 2026  
**الجامعة:** جامعة كاراتاي، قونية، تركيا  

---

## 📋 جدول المحتويات
1. [المتطلبات](##المتطلبات)
2. [التثبيت والإعداد](##التثبيت-والإعداد)
3. [تشغيل البرنامج](##تشغيل-البرنامج)
4. [هيكل المشروع](##هيكل-المشروع)
5. [الإصدارات والحدود](##الإصدارات-والحدود)
6. [استكشاف الأخطاء](##استكشاف-الأخطاء)

---

## المتطلبات

### متطلبات النظام
- **أنظمة التشغيل:** Windows 10+, macOS 10.15+, Linux (Ubuntu 20.04+)
- **المعالج:** Intel i5 أو أعلى (أو ما يعادله)
- **الذاكرة:** 8GB على الأقل (16GB موصى به للتجارب الكاملة)
- **مساحة التخزين:** 5GB متاح على الأقل

### متطلبات البرنامج

#### الواجهة الأمامية (Frontend)
```
Node.js          >= 18.0.0
npm              >= 9.0.0
Next.js          14.2.35
React            19
TypeScript       5
```

#### الجزء العملي (Backend Python)
```
Python           >= 3.10
pip              >= 23.0
```

---

## التثبيت والإعداد

### الخطوة 1: استنساخ/فتح المشروع

```bash
# نسخ المشروع من سطح المكتب
cd "C:\Users\imiss\Desktop\DDoS-BSO-Thesis"
```

### الخطوة 2: تثبيت متطلبات Node.js

```bash
# تثبيت المكتبات
npm install --legacy-peer-deps

# التحقق من التثبيت
npm --version
node --version
```

**إذا حدثت مشاكل:**
```bash
# حذف node_modules وإعادة تثبيت
rm -r node_modules package-lock.json
npm install --legacy-peer-deps
```

### الخطوة 3: تثبيت متطلبات Python (اختياري - للتجارب الجديدة)

```bash
# الانتقال إلى مجلد البرامج النصية
cd scripts

# تثبيت المكتبات المطلوبة
pip install -r requirements.txt

# التحقق من التثبيت
pip list
```

**ملاحظة:** إذا كنت تستخدم conda:
```bash
conda create -n ddos-thesis python=3.10
conda activate ddos-thesis
pip install -r requirements.txt
```

---

## تشغيل البرنامج

### تشغيل الواجهة الأمامية (الطريقة الموصى بها)

```bash
# من جذر المشروع
npm run dev
```

ثم افتح المتصفح وانتقل إلى:
```
http://localhost:3000
```

**الواجهة توفر:**
- ✅ لوحة تحكم تفاعلية
- ✅ رسوم بياني شاملة للنتائج
- ✅ تحليل خوارزمية BSO
- ✅ مقارنة الخوارزميات
- ✅ تقارير قابلة للطباعة
- ✅ تصدير النتائج

### تشغيل التجارب الجديدة (Python Scripts)

#### الطريقة الموصى بها (Enhanced):
```bash
cd scripts
python run_experiments_enhanced.py
```

#### الطريقة التقليدية:
```bash
cd scripts
python run_experiments.py
```

**النتائج ستُحفظ في:**
```
./results/
├── experiment_results.json
├── bso_convergence.json
├── model_comparison.json  
├── performance_metrics.json
├── experiment.log
└── figures/
    ├── bso_convergence_analysis.png
    ├── feature_importance_analysis.png
    ├── confusion_matrices.png
    ├── roc_and_comparison.png
    └── ...
```

---

## هيكل المشروع

```
DDoS-BSO-Thesis/
├── app/                           # صفحات Next.js
│   ├── layout.tsx                 # تخطيط الصفحة الرئيسية
│   ├── page.tsx                   # الصفحة الرئيسية
│   └── globals.css                # أنماط عامة
│
├── components/                    # مكونات React
│   ├── bso-visualization.tsx       # تصور خوارزمية BSO
│   ├── ml-classification-panel.tsx # لوحة التصنيف
│   ├── algorithm-comparison-panel.tsx # مقارنة الخوارزميات
│   ├── performance-evaluation-panel.tsx # تقييم الأداء
│   ├── thesis-results-chapter.tsx  # نتائج الأطروحة
│   ├── thesis-defense-qa.tsx       # أسئلة دفاع الأطروحة
│   ├── print-export-panel.tsx      # طباعة وتصدير
│   └── ui/                         # مكونات واجهة المستخدم
│
├── lib/                           # وحدات منطق البرنامج
│   ├── bso-optimizer.ts           # خوارزمية BSO
│   ├── ciciot2023-dataset.ts      # بيانات CICIoT2023
│   ├── ml-classifier.ts           # متصنفات ML
│   ├── performance-evaluator.ts   # تقييم الأداء
│   ├── algorithm-comparator.ts    # مقارنة الخوارزميات
│   └── utils.ts                   # دوال مساعدة
│
├── scripts/                       # برامج Python
│   ├── run_experiments.py          # تشغيل التجارب
│   ├── run_experiments_enhanced.py # تشغيل معزز
│   ├── real_experiment.py          # التجربة الرئيسية
│   ├── bso_feature_analysis.py    # تحليل ميزات BSO
│   ├── model_evaluation.py        # تقييم النماذج
│   └── requirements.txt           # متطلبات Python
│
├── docs/                          # التوثيق
│   ├── thesis-documentation.md    # توثيق الأطروحة
│   ├── yuksek-lisans-tezi-dokumantasyonu.md # توثيق تركي
│   ├── IMPLEMENTATION_GUIDE.md    # دليل التنفيذ
│   └── API_REFERENCE.md           # مرجع API
│
├── public/                        # ملفات عامة
│   └── experiment_results.json    # نتائج التجارب
│
├── styles/                        # أنماط
│   └── globals.css                # أنماط عامة
│
├── package.json                   # متطلبات Node.js
├── tsconfig.json                  # إعدادات TypeScript
├── next.config.mjs                # إعدادات Next.js
└── project.config                 # إعدادات المشروع
```

---

## الإصدارات والحدود

### الإصدارات
- **الإصدار 1.0.0:** إصدار أساسي مع BSO-RF
- **الإصدار 1.1.0:** إضافة مقارنة خوارزميات (PSO, GA, GWO)
- **الإصدار 1.2.0:** إضافة XGBoost و SVM
- **الإصدار 2.0.0:** إصدار نهائي مع واجهة ويب كاملة

### الحدود المعروفة
1. **حجم البيانات:** يدعم حتى 500MB من بيانات التدريب
2. **المتصفح:** يتطلب HTML5 و ES6
3. **الأداء:** التجارب الطويلة قد تستغرق ساعات
4. **الذاكرة:** قد تحتاج مساحة كبيرة في الذاكرة للبيانات الكاملة

---

## استكشاف الأخطاء

### المشكلة: "Port 3000 already in use"
```bash
# الحل 1: استخدم port آخر
npm run dev -- -p 3001

# الحل 2: قتل العملية على port 3000 (Windows)
netstat -ano | findstr :3000
taskkill /PID <PID> /F
```

### المشكلة: "Module not found"
```bash
# تنظيف Cache
rm -rf .next node_modules
npm install --legacy-peer-deps
npm run dev
```

### المشكلة: شاشة بيضاء في المتصفح
1. افتح الـ DevTools (F12)
2. تحقق من Console للأخطاء
3. الجرت Cache (Ctrl+Shift+Delete)
4. حاول مرة واحدة

### المشكلة: أخطاء Python
```bash
# تحقق من إصدار Python
python --version

# تحديث pip
python -m pip install --upgrade pip

# إعادة تثبيت المكتبات
pip install --force-reinstall -r requirements.txt
```

---

## النقاط المهمة للدفاع

### النقاط التقنية للشرح:
1. **BSO-Hybrid Innovation:**
   - تحسين مشترك لاختيار الميزات وهايبرباراميترات RF
   - بخلاف PSO/GA/GWO التقليدية (اختيار ميزات فقط)

2. **النتائج الرئيسية:**
   - دقة 89.82% مع 51.3% تقليل أبعاد
   - أداء أفضل من PSO-RF (+2.42% F1-Macro)
   - مماثل لـ XGBoost لكن بأقل أبعاد

3. **الإعدادات:**
   - حجم السكان: 25 خفاشاً
   - التكرارات: 50
   - صيغة اللياقة: 0.9×دقة + 0.1×انضغاط

---

## الدعم والإبلاغ عن الأخطاء

**في حالة العثور على خطأ:**
1. اشرح الخطوات المؤدية لهذا الخطأ
2. أرسل رسالة الخطأ الكاملة
3. أضف لقطة شاشة إذا أمكن
4. ذكر إصدار البرنامج والنظام

---

## المراجع

- Yang, X.-S. (2010). "A New Metaheuristic Bat-Inspired Algorithm"
- CICIoT2023 Dataset: https://www.unb.ca/cic/
- Scikit-learn Documentation: https://scikit-learn.org/
- Next.js Documentation: https://nextjs.org/docs

---

**آخر تحديث:** 26 فبراير 2026  
**الحالة:** ✅ جاهز للنشر
